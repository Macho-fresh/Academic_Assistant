/**
 * courses.js — frontend-only interactions for course_list.html.
 *
 * Mobile sidebar toggling is handled globally by app-shell.js. Add Course
 * and View Course are now plain links to real pages, and the search field
 * submits its own GET form natively, so no page-specific JS is needed here.
 */
